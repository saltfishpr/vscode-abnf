{
  "name": "vscode-abnf",
  "displayName": "vscode-abnf",
  "description": "Syntax highlighting, Go to Definition, Find References, Hover, and Auto-completion for ABNF (Augmented Backus-Naur Form) files (RFC 5234 / RFC 7405)",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/saltfishpr/vscode-abnf.git"
  },
  "main": "./out/extension.js",
  "engines": {
    "vscode": "^1.109.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "activationEvents": [
    "onLanguage:abnf"
  ],
  "contributes": {
    "languages": [
      {
        "id": "abnf",
        "aliases": [
          "ABNF",
          "abnf"
        ],
        "extensions": [
          ".abnf"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "abnf",
        "scopeName": "source.abnf",
        "path": "./syntaxes/abnf.tmLanguage.json"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "pnpm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "npx vsce package --no-dependencies",
    "publish": "npx vsce publish"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "@types/vscode": "^1.109.0",
    "typescript": "^5.9.3",
    "vscode-languageclient": "^9.0.1"
  },
  "dependencies": {
    "tree-sitter": "^0.25.0",
    "tree-sitter-abnf": "github:saltfishpr/tree-sitter-abnf",
    "web-tree-sitter": "^0.26.5"
  }
}